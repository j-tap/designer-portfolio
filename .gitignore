############################
# OS X
############################

.DS_Store
.AppleDouble
.LSOverride
Icon
.Spotlight-V100
.Trashes
._*

############################
# Linux
############################

*~

############################
# Windows
############################

Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msm
*.msp

############################
# Misc.
############################

*#
ssl
.idea
nbproject
public/uploads/*
!public/uploads/.gitkeep

############################
# Node.js
############################

lib-cov
lcov.info
pids
logs
results
node_modules
.node_history
.env

# Written by `make merge_env` from backend/.env and frontend/.env, so it holds the Strapi
# signing keys and the SMTP password. It is only ever an intermediate file.
.env.merged

# Generated by `make generate_traefik` with the live domain substituted in.
docker-compose.traefik.yml

codealike.json
